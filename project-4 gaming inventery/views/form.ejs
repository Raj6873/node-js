<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Game Details Form</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #667eea, #764ba2);
      margin: 0;
    }

    .container-box {
      background: white;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 600px;
    }

    .btn-custom {
      background: #764ba2;
      color: white;
      border: none;
    }

    .btn-custom:hover {
      background: #5a3a85;
    }

    .form-control:focus,
    .form-select:focus {
      /* border-color: #4d4d4d !important; */
      box-shadow: none !important;
    }

    .preview-img {
      margin-top: 10px;
      max-width: 100%;
      height: auto;
      display: none;
      border-radius: 8px;
    }
  </style>
</head>
<body>

  <div class="container-box">
    <h3 class="mb-4 text-center">Add Game Details</h3>
    <form method="POST" action="/insert" enctype="multipart/form-data">

      <div class="mb-3">
        <label class="form-label">Game Name</label>
        <input type="text" class="form-control" name="game_name" placeholder="Enter game name" required>
      </div>

      <div class="mb-3">
        <label class="form-label">game_date</label>
        <input type="date" class="form-control" name="game_date" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Game Genre</label>
       
          <select class="form-select" name="game_gener" required>

          <option value="">Select genre</option>
          <option value="Action">Action</option>
          <option value="Adventure">Adventure</option>
          <option value="Sports">Sports</option>
        </select>
      </div>

      <div class="mb-3">
        <label class="form-label">Game Cover</label>
        <input type="file" class="form-control" name="game_image">
        <img id="coverPreview" class="preview-img" alt="Cover Preview">
      </div>

      <button type="submit" class="btn btn-custom w-100">Submit</button>

      <a href="/">back page</a>

    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>