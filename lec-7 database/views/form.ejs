<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Information Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f7f9fc;
            font-family: 'Arial', sans-serif;
        }
        .card {
            border-radius: 1rem;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            background-color: #0069d9;
            color: white;
            text-align: center;
            font-size: 1.25rem;
            padding: 1.5rem;
            border-radius: 0.8rem 0.8rem 0 0;
        }
        .form-control:focus {
            box-shadow: 0 0 0 0.25rem rgba(0, 123, 255, 0.25);
            border-color: #0069d9;
        }
        .btn-primary {
            background-color: #0069d9;
            border: none;
            padding: 0.75rem 1.25rem;
            font-size: 1.1rem;
        }
        .btn-primary:hover {
            background-color: #0056b3;
        }
        .mb-3 {
            margin-bottom: 1.5rem;
        }
        .form-label {
            font-weight: bold;
            color: #333;
        }
        .form-control {
            font-size: 1rem;
            padding: 1rem;
        }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h4>Game Information</h4>
                </div>
                <div class="card-body">
                    <form method="post" action="/studOperation" >
                        <div class="mb-3">
                            <label for="gameNameInput" class="form-label">Game_Name</label>
                            <input type="text" class="form-control" id="gameNameInput" placeholder="Enter the Game Name" required>
                        </div>
                        <div class="mb-3">
                            <label for="gameGenreInput" class="form-label">Game_Genre</label>
                            <input type="text" class="form-control" id="gameGenreInput" placeholder="Enter the Game Genre" required>
                        </div>
                        <div class="mb-3">
                            <label for="gameGenreInput" class="form-label">Game_date</label>
                            <input type="date" class="form-control" id="gameGenreInput" placeholder="Enter your date" required>
                        </div>
                        <div class="mb-3">
                            <label for="gameGenreInput" class="form-label">Game_Genre</label>
                            <input type="text" class="form-control" id="gameGenreInput" placeholder="Enter the Game Genre" required>
                        </div>

                        <div class="mb-3">
                            <label for="gameGenreInput" class="form-label">Game_Image</label>
                            <input type="file" class="form-control" id="gameGenreInput" placeholder="Enter the Game Genre" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Submit</button>
                    </form>

                    <a href="/fetch">Show game</a>

                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
